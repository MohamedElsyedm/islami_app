import 'package:flutter/material.dart';

class RecitersTabBarView extends StatelessWidget {
  const RecitersTabBarView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('reciters'));
  }
}
